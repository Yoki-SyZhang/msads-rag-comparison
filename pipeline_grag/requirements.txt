# ============================================================
# MSADS RAG Agent — Python Requirements
# 安装命令：
#   pip install -r requirements.txt
#
# 推荐环境：Python 3.10+
# ============================================================


# ── Core: retrieval & knowledge graph ───────────────────────
numpy>=1.24
beautifulsoup4>=4.12
chromadb>=0.5
sentence-transformers>=2.7
scikit-learn>=1.3


# ── Agent API server + DeepSeek client ───────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
pydantic>=2.0
openai>=1.0
python-dotenv>=1.0


# ── Evaluation: RAGAS ────────────────────────────────────────
ragas>=0.2
langchain-community>=0.2
tqdm>=4.66
pandas>=2.0
jupyter>=1.0


# ── Scraping (仅重新抓取时需要) ──────────────────────────────
requests>=2.31
playwright>=1.44
# 首次使用 playwright 还需运行：
#   playwright install chromium

# DeepSeek:
#   set DEEPSEEK_API_KEY before running the agent or comparison UI.
